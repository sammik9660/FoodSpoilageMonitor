현재 코드는 **센서→BLE→Wi-Fi→클라우드 연결을 검증하는 프로토타입으로는
타당하지만, 손실을 추적할 수 있는 실험 데이터 수집기로는 아직
부족합니다.** 특히 다음 세 가지를 먼저 해결해야 합니다.

1.  센서 측정 실패가 정상 측정값으로 전달됩니다.
2.  업로드 실패 시 샘플을 복구할 수 없습니다.
3.  화면의 `ONLINE`이 실제 센서 동작 상태를 나타내지 않습니다.

리뷰 대상은 baseline `90e5cc78ead2af1474672beb6b20956ad87cc9b7`입니다.
추적 파일 7개와 기존 참고자료를 검토했고, 코드 원본과 복사본의 동일성도
재확인했습니다. **코드·문서·커밋은 변경하지 않았으며 작업 트리는
깨끗합니다.**

아래에서 **확인**은 코드 또는 모의 실행으로 확인한 사항, **조건부**는
특정 장애·배포 설정에서 발생할 위험, **미검증**은 실제 하드웨어 확인이
필요한 사항입니다. 현재 증거만으로 Critical이라고 단정할 결함은 없으며,
데이터 신뢰성과 보안에 영향을 주는 항목을 High로 평가했습니다.

**1. 잘 설계된 부분**

  ------------------------------------------------------------------------------------------------------------------------
  위치                                                             평가 및 이유        효과와 남은 한계  심각도 / 최소
                                                                                                         수정안
  ---------------------------------------------------------------- ------------------- ----------------- -----------------
  [nRF `loop()`](../../firmware/nrf52840/bme688_node.ino#L239),    **확인:** BLE chunk 한 샘플이 여러    **Low --- 정상
  [ESP32                                                           경계와 샘플 경계를  notification에    경로 설계는
  `notifyCallback()`](../../firmware/esp32s3/gateway.ino#L122)     분리했습니다.       나뉘어도 수신     유지.** 아래
                                                                   ESP32가 줄바꿈까지  가능합니다. 다만  2번의 재동기화
                                                                   조립하는 구조는     연결 단절·부분    처리만
                                                                   적절합니다.         전송 실패까지     보강합니다.
                                                                                       처리하지는        
                                                                                       않습니다.         

  [ESP32                                                           **확인:** 공유 센서 온도는 새 샘플,   **Low --- 유지.**
  `parseSensorData()`](../../firmware/esp32s3/gateway.ino#L106),   구조체를 짧은       습도는 이전       향후 큐로 바꿀
  [`uploadToGoogle()`](../../firmware/esp32s3/gateway.ino#L279)    critical section    샘플처럼 섞이는   때도 네트워크
                                                                   안에서              문제를 줄입니다.  처리를 잠금 안에
                                                                   갱신·복사합니다.    HTTP 호출을 잠금  넣지 않습니다.
                                                                                       밖에 둔 점도      
                                                                                       적절합니다.       

  [ESP32 `setupWiFi()`](../../firmware/esp32s3/gateway.ino#L346),  **확인:**           시리얼 모니터가   **Low --- 구조
  [`setup()`](../../firmware/esp32s3/gateway.ino#L416)             WiFiManager         없다는 이유로     유지.** 설정용 AP
                                                                   설정창과 USB 시리얼 영구 대기하는     credential과 로그
                                                                   대기에 시간 제한이  문제를 피합니다.  노출은 별도
                                                                   있습니다.           Wi-Fi             수정합니다.
                                                                                       credential을      
                                                                                       펌웨어에 직접     
                                                                                       넣는 방식보다     
                                                                                       운용성이          
                                                                                       낫습니다.         

  [Apps Script                                                     **확인:** 필드를    쉼표·따옴표가     **Low --- 유지.**
  `getCsvData()`](../../cloud/apps_script/Code.gs#L193)            따옴표로 감싸고     들어간 값 때문에  날짜 표현을
                                                                   내부 따옴표를       CSV 열이 깨지는   명시적으로
                                                                   이스케이프합니다.   문제를            통일하면 됩니다.
                                                                                       방지합니다.       
  ------------------------------------------------------------------------------------------------------------------------

**2. 정상 환경에서는 동작하지만 취약한 부분**

**2-1. 수신 프레임 재동기화와 파서 검증이 불충분합니다 --- High**

-   **위치:** [ESP32
    `parseSensorData()`](../../firmware/esp32s3/gateway.ino#L93),
    [`notifyCallback()`](../../firmware/esp32s3/gateway.ino#L122),
    [`onDisconnect()`](../../firmware/esp32s3/gateway.ino#L156)
-   **확인:** 연결이 끊겨도 `rxBuffer`를 초기화하지 않습니다. 길이가
    200을 넘으면 즉시 비운 뒤 같은 프레임의 나머지를 다시 수집합니다.
    `sscanf()`는 숫자 네 개만 읽히면 성공으로 판단하고 뒤에 남은 문자를
    검사하지 않습니다.
-   **증상:** 끊기기 직전의 부분 문자열과 재연결 후 샘플이 붙거나,
    손상된 프레임 일부가 정상 샘플로 처리될 수 있습니다.
-   **검증:** 호스트 C 런타임에서 동일 포맷으로
    `T=25,H=50,P=1000,G=123junk`가 네 값 파싱 성공으로 처리됨을
    확인했습니다. ESP32에서의 실행 재현은 하지 않았습니다.
-   **최소 수정안:** 연결 세대가 바뀔 때 수신 버퍼를 초기화하고,
    overflow 후에는 다음 줄바꿈까지 폐기합니다. 파싱 종료 위치와 유한수
    여부, 필드별 허용 범위를 검사합니다.

**2-2. BLE 송신 성공을 확인하지 않습니다 --- Medium**

-   **위치:** [nRF `loop()`의
    `bleuart.write()`](../../firmware/nrf52840/bme688_node.ino#L246)
-   **확인:** 데이터 chunk와 마지막 줄바꿈의 반환값을 무시하고 `TX`
    로그를 출력합니다.
-   **조건부:** 로컬 Adafruit nRF52 1.7.0 구현에서는 notification
    미활성화나 송신 실패 시 `write()`가 0을 반환할 수 있습니다. 실제
    baseline 빌드 버전은 고정되어 있지 않습니다.
-   **증상:** 연결 직후 구독이 완료되기 전, 또는 전송 중 연결이 끊겼을
    때 일부 chunk만 전달되어도 송신 성공처럼 보입니다.
-   **최소 수정안:** 반환 길이를 확인하고 실패 카운터를 남깁니다.
    재전송은 부분 문자열이 아니라 식별 가능한 완전한 샘플 단위로
    설계해야 합니다.

**2-3. 업로드 성공 판정이 JSON 문자열 모양에 의존합니다 --- Medium**

-   **위치:** [ESP32
    `uploadToGoogle()`](../../firmware/esp32s3/gateway.ino#L330)
-   **확인:** `"success":true`라는 정확한 부분 문자열을 찾습니다.
-   **증상:** 정상 서버가 `{"success": true}`처럼 공백을 넣어도 실패로
    판단합니다. 반대로 응답의 전체 구조나 실제 저장된 샘플 ID는 확인하지
    않습니다.
-   **최소 수정안:** JSON을 파싱해 명시적인 성공 필드와 수신 확인 ID를
    검사합니다.

리다이렉트 추적 자체는 필요한 처리입니다. Apps Script ContentService가
별도 URL로 응답을 전달하기 때문입니다. 설치된 ESP32 core 3.3.11
소스에서는 302/303을 GET으로 바꾸는 처리도 확인했으므로, 현재 근거로
"리다이렉트 때문에 반드시 중복 POST된다"고 판단하지는 않습니다. [Google
공식 설명](https://developers.google.com/apps-script/guides/content)

**2-4. 클라우드 저장소 자동 생성에 동시 실행 보호가 없습니다 ---
Medium**

-   **위치:** [Apps Script
    `getSensorSheet()`](../../cloud/apps_script/Code.gs#L91)
-   **확인:** `SPREADSHEET_ID` 확인→새 Spreadsheet 생성→ID 저장 과정에
    잠금이 없습니다.
-   **조건부 증상:** 초기 POST와 CSV 요청 등이 동시에 들어오면 서로 다른
    Spreadsheet를 만들고, 최종 저장된 ID가 한쪽만 가리킬 수 있습니다.
    일부 행이 다른 파일에 남을 수 있습니다.
-   **최소 수정안:** 초기화 구간에 script lock을 적용하거나, 검증된
    Spreadsheet ID를 사전 설정합니다. 모든 `appendRow()`가 반드시
    충돌한다고 주장하는 것은 아닙니다. 문제는 여러 단계로 이루어진
    초기화입니다. [Google
    LockService](https://developers.google.com/apps-script/reference/lock)

**3. 명확한 버그 또는 데이터 손실 가능성**

**3-1. 센서 오류가 정상 데이터로 위장됩니다 --- High**

-   **위치:** [nRF
    `loop()`](../../firmware/nrf52840/bme688_node.ino#L175), [프레임
    생성·송신](../../firmware/nrf52840/bme688_node.ino#L214)
-   **확인:** 센서 초기화가 실패하면 측정값은 초기값 0으로 남습니다.
    이후 측정이 실패하면 이전 값이 남습니다. 두 경우 모두 BLE 송신은
    계속됩니다.
-   **증상:** 센서가 빠져도 `0,0,0,0`이 반복 저장되거나, 접촉 불량 이후
    마지막 값이 계속 새 데이터처럼 저장됩니다. ESP32는 수신 시각을
    갱신하므로 이를 stale 데이터로 걸러내지도 못합니다.
-   **최소 수정안:** 이번 측정의 성공 여부를 별도로 관리하고, 실패한
    측정은 정상 샘플로 송신하지 않습니다. 이후 프로토콜 확장으로 오류
    상태·측정 시각·sequence를 전달합니다.

**3-2. 업로드 전에 샘플을 소모하고, 단절 중 샘플을 보존하지 않습니다 ---
High**

-   **위치:** [ESP32
    `uploadToGoogle()`](../../firmware/esp32s3/gateway.ino#L279),
    [`loop()`](../../firmware/esp32s3/gateway.ino#L447)
-   **확인:** Wi-Fi 확인과 HTTP 요청 전에 `newData=false`로 바꿉니다.
    실패한 스냅샷의 재전송 경로는 없습니다. BLE가 끊기면 업로드 구간에
    도달하기 전에 반환합니다.
-   **증상:** Wi-Fi 순간 단절이나 서버 오류로 해당 샘플이 사라집니다.
    짧게 BLE를 연결해 데이터를 받은 뒤 끊기는 사용 방식에서는, 수신한
    값이 있어도 업로드되지 않고 10초 stale 조건에 걸릴 수 있습니다.
-   **최소 수정안:** 전송 대기 샘플을 별도 보관하고 서버 확인 이후
    제거합니다. 업로드 가능 여부는 현재 BLE 연결 여부와 분리합니다. 여러
    샘플 보존에는 큐가 필요합니다.
-   **주의:** 단순히 성공 후 `newData=false`로 옮기면 HTTP 처리 중
    들어온 새 샘플의 플래그를 지울 수 있습니다. sequence 비교나 큐가
    필요합니다.

**3-3. 클라우드 화면이 실제 상태와 관계없이 `ONLINE`을 표시합니다 ---
Medium**

-   **위치:** [Apps Script
    `getSensorData()`](../../cloud/apps_script/Code.gs#L172), [웹
    `updateSensor()`](../../cloud/apps_script/Code.gs#L397)
-   **확인:** 데이터가 없을 때도 0을 담은 객체를 반환합니다. 브라우저는
    함수 호출이 성공하면 무조건 `ESP32 ONLINE`을 표시합니다.
-   **증상:** 한 번도 업로드하지 않았거나 장치를 꺼둔 상태에서도
    ONLINE이 됩니다. 과거 측정값을 현재값으로 오해할 수 있습니다.
-   **최소 수정안:** `hasData`, 서버 수신 시각, 데이터 나이에 따라
    `WAITING / FRESH / STALE`을 표시합니다. 서버 응답 성공과 센서 정상
    동작을 구분합니다.

**3-4. Sheets 저장 실패와 화면 표시 상태가 불일치합니다 --- High**

-   **위치:** [Apps Script
    `doPost()`](../../cloud/apps_script/Code.gs#L37)
-   **확인·재현:** `SENSOR_DATA`를 먼저 저장한 다음 `appendRow()`를
    실행합니다. 원본 코드를 메모리에서 모의 실행했을 때 `appendRow()`가
    실패해도 최신 표시값은 바뀌었습니다.
-   **증상:** 화면에는 새 데이터가 보이지만 CSV에는 없습니다. 사용자는
    수집이 정상이라고 판단할 수 있습니다.
-   **최소 수정안:** 검증과 행 저장을 먼저 수행하고, 화면용 최신값은
    저장된 데이터 기준으로 갱신합니다. 저장 성공과 캐시 갱신 실패도
    구분해야 합니다.

**3-5. 재시도 시 중복 저장을 막을 식별자가 없습니다 --- High**

-   **위치:** [ESP32 JSON
    생성](../../firmware/esp32s3/gateway.ino#L297), [Apps Script
    `appendRow()`](../../cloud/apps_script/Code.gs#L51)
-   **확인:** 요청·샘플 ID가 없고, 받은 요청마다 행을 추가합니다.
-   **조건부 증상:** 서버는 저장했지만 응답이 유실된 경우, 향후 재시도를
    추가하면 동일 샘플이 중복 저장됩니다.
-   **최소 수정안:** `device_id + boot/session_id + sequence`를 저장
    키로 사용하고, 이미 저장한 키의 재요청은 중복 추가 없이 성공
    응답합니다. 재시도와 중복 방지는 같이 도입해야 합니다.

**4. 유지보수성 문제**

**4-1. 현재 폴더 구조는 표준 Arduino 스케치 규칙과 맞지 않습니다 ---
Medium**

-   **위치:**
    [bme688_node.ino](../../firmware/nrf52840/bme688_node.ino),
    [gateway.ino](../../firmware/esp32s3/gateway.ino)
-   **확인:** 상위 폴더명은 각각 `nrf52840`, `esp32s3`인데 같은 이름의
    주 `.ino`가 없습니다.
-   **증상:** 표준 Arduino CLI에서 현재 폴더를 그대로 빌드하거나 IDE에서
    열 때 폴더 재구성이 필요할 수 있습니다. 이는 코드 로직 결함과 별개인
    빌드 진입점 문제입니다.
-   **최소 수정안:** 향후 `nrf52840/bme688_node/bme688_node.ino`,
    `esp32s3/gateway/gateway.ino`처럼 맞추거나 별도 빌드 구성을
    제공합니다. 이번에는 이동하지 않았습니다. [Arduino 스케치
    규격](https://docs.arduino.cc/arduino-cli/sketch-specification)

**4-2. 재현 가능한 빌드 정보가 부족합니다 --- Medium**

-   **위치:** [README.md](../../README.md#L44), 펌웨어 두 파일의 include
    및 저장소 전체 구성
-   **확인:** nRF의 정확한 보드/FQBN, core·라이브러리 버전, 빌드 명령,
    자동 검증 구성이 없습니다.
-   **증상:** 다른 PC에서 동일 코드를 빌드해도 API 차이로 실패하거나 BLE
    동작이 달라질 수 있습니다.
-   **최소 수정안:** 성공한 실제 환경의 보드·core·라이브러리 버전과
    명령을 기록합니다. 로컬에는 ESP32 3.3.11, Adafruit nRF52 1.7.0,
    BME680 2.0.6, WiFiManager 2.0.17이 있지만 이것이 과거 성공 빌드
    환경과 같다는 근거는 없습니다.

**4-3. 기능 경계는 있지만 검증 가능한 모듈 경계가 약합니다 --- Medium**

-   **위치:** [gateway.ino](../../firmware/esp32s3/gateway.ino#L93),
    [Code.gs](../../cloud/apps_script/Code.gs#L225)
-   **확인:** BLE 파싱, 연결 관리, 저장 상태, HTTP 전송이 전역 상태로
    연결되어 있습니다. Apps Script 서버 처리와 HTML도 한 파일에
    있습니다.
-   **증상:** parser나 재시도만 바꾸려 해도 보드와 클라우드를 모두
    연결해야 검증하기 쉽습니다.
-   **최소 수정안:** 먼저 parser와 입력 검증을 순수 함수로 분리해 경계
    조건을 테스트합니다. 지금 규모에서 대규모 클래스 구조로 바꾸는 것은
    우선순위가 낮습니다.

**4-4. 문서가 현재 Git 상태와 맞지 않습니다 --- Low**

-   **위치:** [PROJECT_STATE.md](../../PROJECT_STATE.md#L20),
    [README.md](../../README.md#L44)
-   **확인:** "Git 초기화 전", "commit 미수행" 상태가 남아 있습니다.
    데이터 단위도 문서에서는 미확정이지만 소스에는 변환과 표시 단위가
    존재합니다.
-   **증상:** 다음 작업자가 현재 상태나 실제 코드 계약을 잘못
    이해합니다.
-   **최소 수정안:** 이후 별도 문서 커밋으로 "코드가 사용하는 단위"와
    "실제 하드웨어 검증 여부"를 분리해 갱신합니다. 기존 baseline 커밋을
    고칠 필요는 없습니다.

**5. 임베디드 관점 문제**

**5-1. I2C 핀 정의가 실제 설정에 반영되지 않습니다 --- High**

-   **위치:** [nRF 핀
    정의](../../firmware/nrf52840/bme688_node.ino#L12),
    [`initBME688()`](../../firmware/nrf52840/bme688_node.ino#L68)
-   **확인:** `BME_SDA_PIN`, `BME_SCL_PIN`은 정의만 있고 사용되지
    않습니다. 실제 연결은 해당 board variant의 기본 `Wire` 핀에
    의존합니다.
-   **미검증 증상:** 주석의 숫자대로 배선해도 센서를 찾지 못할 수
    있습니다. 해당 숫자가 물리 핀인지 Arduino 인덱스인지도 보드 확인
    없이 결정할 수 없습니다.
-   **최소 수정안:** 정확한 보드·variant·회로도와 I2C 스캔으로 먼저
    검증한 뒤 문서와 설정을 일치시킵니다. 핀 번호를 추측해서 바꾸면 안
    됩니다.

**5-2. 센서 초기화 실패 후 복구 경로가 없습니다 --- Medium**

-   **위치:** [nRF
    `initBME688()`](../../firmware/nrf52840/bme688_node.ino#L76),
    [`setup()`](../../firmware/nrf52840/bme688_node.ino#L125)
-   **확인:** 초기화는 부팅 시 한 번만 수행합니다. `bmeReady=false` 이후
    재시도가 없습니다.
-   **증상:** 전원 인가 타이밍이나 포고핀 접촉 때문에 최초 초기화가
    실패하면, 접촉을 복구해도 재부팅 전까지 측정하지 않습니다.
-   **최소 수정안:** 제한된 주기의 재초기화와 연속 오류 카운터를
    추가합니다. 무제한 빠른 재시도나 무조건 재부팅은 피합니다.

**5-3. 네트워크와 센서 검색이 제어 루프를 오래 점유합니다 --- Medium**

-   **위치:** [ESP32
    `scanForSensor()`](../../firmware/esp32s3/gateway.ino#L210),
    [`uploadToGoogle()`](../../firmware/esp32s3/gateway.ino#L312),
    [`setup()`](../../firmware/esp32s3/gateway.ino#L428)
-   **확인:** 동기 스캔·HTTP 처리 중 `loop()`의 버튼 검사가 진행되지
    않습니다. Wi-Fi 설정이 끝난 뒤 BLE를 시작합니다.
-   **증상:** BOOT 버튼의 "3초 길게 누르기" 반응이 늦거나 입력을 놓칠 수
    있습니다. Wi-Fi가 준비되지 않은 부팅 상황에서는 센서 수신도 시작하지
    못합니다.
-   **최소 수정안:** BLE 수신 시작과 Wi-Fi 연결을 분리하고, 이후 필요에
    따라 비동기 스캔이나 전송 태스크를 도입합니다.
-   **구분:** BLE callback까지 반드시 멈춘다는 뜻은 아닙니다. 콜백은
    별도 스택 문맥에서 실행될 수 있습니다.

**5-4. 2초 주기와 저전력 동작은 아직 검증된 특성이 아닙니다 --- Medium**

-   **위치:** [nRF
    `performReading()`](../../firmware/nrf52840/bme688_node.ino#L177),
    [`delay(2000)`](../../firmware/nrf52840/bme688_node.ino#L268)
-   **확인:** 실제 주기는 측정·로그·송신 소요 시간에 2초를 더한
    값입니다.
-   **미검증 증상:** 정확한 2초 간격을 전제로 분석하면 시간축이
    어긋납니다. 히터·광고·보드 주변 회로를 포함한 전류가 측정되지 않아
    배터리 수명을 예측하기 어렵습니다.
-   **최소 수정안:** 측정 시각과 실행 시간을 기록하고, 절대 주기
    스케줄링 및 실제 전류 측정을 진행합니다. `delay()`가 항상 CPU를
    바쁘게 돌린다고 단정할 수는 없습니다.

**5-5. 가스 값의 유효성과 heater 안정 상태가 전달되지 않습니다 --- High,
ML 수집 기준**

-   **위치:** [nRF 측정·가스값
    사용](../../firmware/nrf52840/bme688_node.ino#L177)
-   **확인:** 전송 데이터에는 가스 측정 유효성·heater 안정 상태가
    없습니다.
-   **조건부:** 설치된 Adafruit_BME680 2.0.6 구현에서는 가스 상태에 따라
    `gas_resistance=0`이 되더라도 `performReading()`이 성공할 수
    있습니다.
-   **증상:** 센서 안정화 과정이나 유효하지 않은 가스 측정을 실제 낮은
    저항값으로 학습할 수 있습니다.
-   **최소 수정안:** 사용 버전의 상태 처리부터 확인하고, 실험 데이터에
    유효성 정보를 남깁니다. heater-profile 수집으로 전환할 때 해당
    상태와 profile step을 명시적으로 보존합니다.

**6. 네트워크·보안 문제**

**6-1. HTTPS 서버 인증을 하지 않습니다 --- High**

-   **위치:** [ESP32
    `client.setInsecure()`](../../firmware/esp32s3/gateway.ino#L308)
-   **확인:** 서버 인증서 검증을 생략합니다.
-   **조건부 증상:** 악성 AP나 중간자 환경에서 업로드 내용이
    노출·변조되거나 가짜 성공 응답을 받을 수 있습니다.
-   **최소 수정안:** CA 검증과 필요한 시각 동기화를 적용하고 Apps Script
    리다이렉트 대상까지 검증합니다. 실험실 prototype의 임시 설정이라는
    표시만으로 제품 보안이 충족되지는 않습니다.

**6-2. 설정 AP 비밀번호가 소스·SSID·로그에 노출됩니다 --- High**

-   **위치:** [ESP32 AP 설정](../../firmware/esp32s3/gateway.ino#L33),
    [시리얼 출력](../../firmware/esp32s3/gateway.ino#L360)
-   **확인:** 비밀번호가 상수이며 AP 이름에도 포함되어 있습니다. 현재
    baseline 커밋에도 들어 있습니다.
-   **증상:** 설정 AP가 열렸을 때 주변 사람이 SSID만 보고 접속
    비밀번호를 알 수 있습니다.
-   **최소 수정안:** AP 이름과 로그에서 비밀번호를 제거하고 장치별
    설정값으로 분리합니다. **새 커밋에서 제거해도 기존 커밋의 값은
    남으므로, 공개 전에 실제 사용하는 값을 교체해야 합니다.** 이번에는
    baseline이나 credential을 변경하지 않았습니다.

**6-3. API 입력 검증과 애플리케이션 인증이 없습니다 --- High**

-   **위치:** [Apps Script
    `doPost()`](../../cloud/apps_script/Code.gs#L18),
    [`doGet()`](../../cloud/apps_script/Code.gs#L225)
-   **확인·재현:** `Number()` 변환만 수행합니다. 모의 실행에서 `null`,
    빈 문자열, `true`가 각각 `0`, `0`, `1`로 저장되고 성공 응답을
    받았습니다.
-   **조건부 증상:** 배포가 익명 접근을 허용한다면 외부인이 가짜
    측정값을 넣거나 호출량을 늘릴 수 있습니다. CSV 접근 범위 역시 실제
    배포 권한에 달려 있습니다.
-   **최소 수정안:** JSON 스키마·필수 필드·숫자 타입·유한수·범위를
    검사하고, 장치 인증과 요청 크기 제한을 적용합니다. 읽기 권한과 쓰기
    권한도 분리합니다.
-   **구분:** 실제 배포가 익명 공개인지, 현재 악용되는지는 확인하지
    않았습니다. URL 자체를 API key로 간주해서는 안 됩니다.

**6-4. BLE 서비스 UUID를 장치 신원처럼 사용합니다 --- Medium**

-   **위치:** [ESP32
    `onResult()`](../../firmware/esp32s3/gateway.ino#L173)
-   **확인:** NUS UUID를 광고하는 첫 장치를 선택합니다. 대상 센서의 등록
    정보나 인증을 검사하지 않습니다.
-   **증상:** 주변의 다른 NUS 장치에 붙거나, 동일 UUID를 광고하는 장치가
    가짜 센서 데이터를 주입할 수 있습니다.
-   **최소 수정안:** 실험 단계에서는 대상 장치 등록·필터링을 추가합니다.
    제품 단계에서는 페어링/본딩과 인증을 검토합니다. MAC 필터만으로 강한
    인증이 되지는 않습니다.

**7. 데이터 수집·ML 관점 문제**

**7-1. 현재 구조는 최신값 모니터이며 전체 시계열 수집기가 아닙니다 ---
High**

-   **위치:** [ESP32
    `latest = d`](../../firmware/esp32s3/gateway.ino#L107), [30초
    주기](../../firmware/esp32s3/gateway.ino#L77)
-   **확인:** 수신할 때마다 이전 샘플을 덮어씁니다.
-   **증상:** 측정이 정확히 2초 간격이라고 가정하면 30초의 약 15개 중
    1개만 남습니다. 이는 평균·필터링이 아니라 마지막 값 선택이라 짧은
    변화가 사라집니다. 실제 측정 간격은 2초보다 길 수 있습니다.
-   **최소 수정안:** ML 수집 전에 bounded ring buffer와 batch 업로드를
    도입하고 overflow·누락 개수를 기록합니다. 큐가 있다는 사실만으로
    전원 손실까지 보호되지는 않습니다.

**7-2. 측정 시각·샘플 식별자·실험 식별자가 없습니다 --- High**

-   **위치:** [nRF 프레임
    생성](../../firmware/nrf52840/bme688_node.ino#L216), [ESP32
    JSON](../../firmware/esp32s3/gateway.ino#L297), [Sheets
    열](../../cloud/apps_script/Code.gs#L138)
-   **확인:** 저장하는 시간은 서버 도착 시각입니다. `device_id`,
    `experiment_id`, `session_id`, `sequence`, 장치 측정 시각이
    없습니다.
-   **증상:** 업로드 지연을 센서 변화로 오해하고, 재부팅·중복·누락을
    구분하지 못합니다. 문서의 session 단위 train/test 분리를
    데이터만으로 수행하기 어렵습니다.
-   **최소 수정안:** 최소한 실험·세션·장치 ID, boot ID, sequence,
    monotonic 측정 시각과 서버 수신 시각을 분리해 기록합니다. nRF에 바로
    UTC 시계를 요구할 필요는 없습니다.

**7-3. 정답과 실험 조건을 보존할 구조가 없습니다 --- High, 부패 분류
주장 기준**

-   **위치:** [AGENTS.md ML 규칙](../../AGENTS.md#L29), [Apps Script
    저장 스키마](../../cloud/apps_script/Code.gs#L51), 비어 있는
    `analysis/`
-   **확인:** 습도를 label로 쓰지 않는다는 규칙은 있지만 label 정의·관찰
    방식·음식 종류·용기·센서 위치·실험 조건을 저장하는 구현은 없습니다.
-   **증상:** 모델이 부패 대신 음식 종류, 날짜, 센서 편차, 온습도 조건을
    구분해도 높은 정확도로 보일 수 있습니다.
-   **최소 수정안:** 데이터 수집 전에 experiment manifest와 정답 부여
    절차를 정합니다. 전처리와 정규화도 train 데이터로만 학습하고, 같은
    음식 실험에서 파생된 세션이 양쪽에 섞이지 않도록 묶음 기준을 정해야
    합니다.

**7-4. 현재 네 값으로 이후 heater-profile 데이터를 대체할 수 없습니다
--- Medium**

-   **위치:** [nRF heater
    설정](../../firmware/nrf52840/bme688_node.ino#L100), [소수점 두 자리
    직렬화](../../firmware/nrf52840/bme688_node.ino#L219)
-   **확인:** 단일 heater 설정 아래의 네 개 수치만 전달하고 profile
    step·gas 상태·센서 설정 버전을 보존하지 않습니다.
-   **증상:** 이후 AI-Studio/BSEC 또는 heater-profile 기반 실험으로
    넘어가도 기존 CSV에서 필요한 정보를 복원할 수 없습니다.
-   **최소 수정안:** 현재 데이터를 "기본 센서 테스트 데이터"로
    구분합니다. 이후 별도 버전의 수집 스키마에서 heater profile, 상태,
    정밀도를 정의합니다. 현재 단위를 임의로 바꾸면 안 됩니다.

**8. 학부 캡스톤 수준에서 충분한 부분**

프로젝트 범위를 **"BME688 센서 데이터를 BLE 게이트웨이로 전달하고 웹에
시각화하는 프로토타입"**으로 잡으면 구성은 충분히 적절합니다. 다만 현재
기록만으로 실제 센서부터 CSV까지 검증을 완료했다고 주장할 수는 없습니다.

  -----------------------------------------------------------------------------------------------------------
  평가 범위 / 위치                                      충분한 이유       발생 가능한       심각도 / 최소
                                                                          평가상 문제       보완
  ----------------------------------------------------- ----------------- ----------------- -----------------
  [nRF                                                  서로 다른 MCU     dummy 통신 성공을 **Medium.** 실제
  송신](../../firmware/nrf52840/bme688_node.ino#L230) → 사이에 NUS와      실제 센서 통신    측정값과
  [ESP32 수신](../../firmware/esp32s3/gateway.ino#L122) framing을 구현한  성공으로 혼동할   원본·수신 로그를
                                                        것은 적절한 통합  수 있습니다.      대응시킨 검증
                                                        과제입니다.                         기록을
                                                                                            추가합니다.

  [ESP32                                                서버 운영 부담    화면이 보인다는   **Medium.** 수신
  업로드](../../firmware/esp32s3/gateway.ino#L272) →    없이 end-to-end   사실만으로 누적   건수·저장 행
  [Apps Script](../../cloud/apps_script/Code.gs#L18)    데모를 구성할 수  저장·CSV까지      수·CSV 행 수를
                                                        있습니다.         성공했다고 판단할 비교합니다.
                                                                          수 있습니다.      

  [PROJECT_STATE.md](../../PROJECT_STATE.md#L55),       확인/미확인       문서의 원칙이     **Medium.** 실험
  [AGENTS.md](../../AGENTS.md#L27)                      사항과 ML 누수    실험 절차나       계획과 session
                                                        방지 규칙을       데이터 필드로     기반 평가 결과를
                                                        명시했습니다.     구현되지          증거로 남깁니다.
                                                                          않았습니다.       
  -----------------------------------------------------------------------------------------------------------

**"음식 부패를 정확하게 판정한다"는 캡스톤 목표라면 현재는 충분하지
않습니다.** 부족한 핵심은 UI 기능 수가 아니라 독립적인 정답, 여러 실험의
반복, 수집 신뢰성, 실험 단위 일반화 평가입니다.

**9. 제품 수준으로 가려면 필요한 개선**

  -------------------------------------------------------------------------------------------------------------------
  우선순위 / 위치                                               필요한 개선과     현재 구조의 예상  심각도 / 가장
                                                                이유              증상              작은 시작점
  ------------------------------------------------------------- ----------------- ----------------- -----------------
  1 --- [nRF 측정                                               정상값·측정       고장난 센서가     **High.** 실패
  루프](../../firmware/nrf52840/bme688_node.ino#L169)           실패·오래된 값을  정상값처럼        샘플 송신 차단과
                                                                구별해야 합니다.  보입니다.         상태 카운터부터
                                                                                                    추가

  2 --- [ESP32                                                  재시도·수신       누락과 중복을     **High.** ID가
  업로드](../../firmware/esp32s3/gateway.ino#L272), [클라우드   확인·중복         설명할 수         있는 큐와
  저장](../../cloud/apps_script/Code.gs#L18)                    방지·단절 저장을  없습니다.         idempotent
                                                                하나의 전달                         저장부터 구현
                                                                계약으로 만들어야                   
                                                                합니다.                             

  3 --- [ESP32 보안                                             장치 인증, TLS    가짜 데이터·설정  **High.**
  설정](../../firmware/esp32s3/gateway.ino#L308), [API          검증,             변경·통신 변조    credential
  진입점](../../cloud/apps_script/Code.gs#L18)                  provisioning과    위험이 남습니다.  교체·분리와 서버
                                                                credential 교체가                   입력 검증부터
                                                                필요합니다.                         수행

  4 --- [프로젝트 구성](../../README.md#L15)                    고정 빌드, 배포   현장 문제를 같은  **Medium.**
                                                                버전 추적, 장애   버전으로 재현하기 보드·버전·빌드
                                                                주입 테스트가     어렵습니다.       명령과 firmware
                                                                필요합니다.                         ID 기록

  5 --- [CSV 전체 조회](../../cloud/apps_script/Code.gs#L193),  데이터 증가와     전체 시트         **Medium.**
  [웹 polling](../../cloud/apps_script/Code.gs#L437)            동시 사용자 수에  읽기·문자열       기간별 조회·분할
                                                                따른 한계를       생성과 반복 조회  export·요청
                                                                측정해야 합니다.  비용이 계속       계측부터 적용
                                                                                  증가합니다.       

  6 --- [nRF                                                    전력, 결로, RF    배터리            **High, 현장 운용
  heater·주기](../../firmware/nrf52840/bme688_node.ino#L100),   단절, 전원 이상,  소진·간헐적 I2C   기준.** 전류
  [ESP32 설정](../../PROJECT_STATE.md#L78)                      장기 운전 및 현장 오류·복구 불능이  측정과 단절·전원
                                                                업데이트 계획이   생길 수 있습니다. 복구 시험부터
                                                                필요합니다.                         진행
  -------------------------------------------------------------------------------------------------------------------

제품화를 위해 바로 클라우드 전체를 교체할 필요는 없습니다. 먼저 현재
시스템에서 **어떤 데이터가 생성되고, 무엇이 저장되었고, 무엇이
유실되었는지 설명할 수 있어야 합니다.**

**내가 이 프로젝트를 처음부터 다시 설계한다면 어떤 구조로 만들 것인가**

핵심 원칙은 **샘플을 만들 때 ID와 유효성을 부여하고, 저장 확인을 받기
전까지 전달 책임을 유지하는 것**입니다.

``` mermaid
flowchart LR
    A[BME688 측정·상태 확인] --> B[nRF 샘플 생성·버퍼]
    B -->|BLE NUS / 버전 있는 프레임| C[ESP32 검증·수신 큐]
    C --> D[단절 보존·batch 전송]
    D -->|검증된 TLS와 장치 인증| E[입력 검증·중복 방지 API]
    E --> F[실험 데이터 저장]
    F --> G[최신 상태 화면]
    F --> H[CSV·Colab·실험 단위 평가]
```

-   **nRF52840:** `SensorDriver`, `Sampler`, `FrameEncoder`,
    `BleTransport` 정도로 나눕니다. 측정 성공과 전송 성공을 구분하고,
    실제 연결 단절 시간이 어느 정도인지 측정해 센서 측 버퍼 용량을
    정합니다. I2C 핀은 검증한 보드 설정 한 곳에서 관리합니다.
-   **ESP32-S3:** BLE 수신은 네트워크 상태와 독립적으로 진행합니다.
    parser가 검증한 샘플을 큐에 넣고, uploader가 batch로 전송합니다. RAM
    큐의 overflow 정책과 전원 손실 시 허용 손실량을 먼저 정한 뒤 필요한
    경우 flash/SD 저장을 추가합니다.
-   **데이터 계약:** `schema_version`, `device_id`, `boot_id`,
    `sequence`, `measured_uptime_ms`, `validity`, 기존 단위의 측정값을
    기본으로 둡니다. 게이트웨이 수신 시각과 서버 수신 시각은 별도
    필드입니다. 실험·음식·용기·heater 설정 정보는 experiment manifest와
    연결합니다.
-   **클라우드:** 인증→검증→중복 확인→저장→수신 확인 순서를 지킵니다.
    Apps Script/Sheets는 초기 저장소로 유지할 수 있지만, UI는 저장된
    결과를 조회하도록 합니다. 배포 ID와 firmware 버전의 대응도
    기록합니다.
-   **분석:** 원본 데이터를 보존하고, 정제본·특징·모델을 분리합니다.
    experiment 단위 split을 먼저 고정한 뒤 전처리를 학습합니다. 모델
    출력에는 성능뿐 아니라 적용 가능한 음식·조건·불확실성을 함께
    정의합니다.
-   **검증:** 프레임 분할, 연결 중단, 센서 오류, 서버 저장 후 응답 유실,
    중복 요청, 큐 초과, 재부팅을 필수 시험으로 둡니다. 장기 시험에서는
    생성·수신·저장·누락 건수를 맞춥니다.

이번 검토에서는 Apps Script 입력 변환과 저장 실패 시 불일치를 모의
실행으로 재현했고, parser의 뒤따르는 문자 허용을 호스트에서
확인했습니다. 실제 보드 컴파일·실행, 배포 권한 확인, 냉장고 환경 시험은
수행하지 않았습니다.
